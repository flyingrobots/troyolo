{
  "help_title": "Troyolo",
  "flags": [
    {
      "name": "config",
      "desc": "Filepath to configuration file.",
      "options": {
        "type": "string",
        "default": "./config/app.json"
      }
    },
    {
      "name": "data",
      "desc": "Directory path where account information will be saved.",
      "options": {
        "type": "string",
        "default": "./data"
      }
    }
  ]
}
